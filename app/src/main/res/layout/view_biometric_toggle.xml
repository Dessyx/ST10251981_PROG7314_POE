<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/biometric_toggle_root"
    android:layout_width="140dp"
    android:layout_height="19dp">

    <View
        android:id="@+id/biometric_track"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/biometric_track_custom" />

    <FrameLayout
        android:id="@+id/biometric_thumb"
        android:layout_width="76dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@drawable/biometric_thumb_custom"
        android:paddingLeft="6dp"
        android:paddingRight="6dp">

        <TextView
            android:id="@+id/biometric_thumb_text"
            android:layout_width="44dp"
            android:layout_height="11dp"
            android:layout_gravity="top|start"
            android:layout_marginTop="4dp"
            android:text="@string/biometric_on"
            android:textSize="10sp"
            android:fontFamily="sans-serif"
            android:textColor="#000000" />
    </FrameLayout>

</FrameLayout>
